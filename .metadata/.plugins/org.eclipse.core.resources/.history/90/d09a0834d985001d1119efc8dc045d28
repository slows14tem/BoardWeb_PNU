<html xmlns:th="http://www.thymeleaf.org"
	xmlns:sec="http://www.thymeleaf.org/thymeleaf-extras-springsecurity5">


<head>
<title>게시글 상세</title>
<meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
<link
	href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
	rel="stylesheet"
	integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD"
	crossorigin="anonymous">
</head>
<body th:align="center">

<main class="mt-5 pt-5">
	<div class="container-fluid px-4">
		<h1 class="mt-4">Board</h1>
		<div class="card mb-4">
			<div class="card-body">
				<form th:action="@{/board/updateBoard}" method="post">
					<div class="mb-3 mt-3">
						<label for="bno" class="form-label">제목</label>
						<input name="seq" type="hidden" th:value="${board.seq}">
						<input name="title" type="text" class="form-control" th:value="${board.title}">
					</div>
					<div class="mb-3">
						<label for="title" class="form-label">작성자</label> <input
							type="text" class="form-control" name="member"
							th:value="${board.member.name}" readonly>
					</div>
					<div class="mb-3">
						<label for="content" class="form-label">내용</label>							
						<textarea class="form-control" name="content" th:text="${board.content}"></textarea>
					</div>
					<div class="mb-3">
						<label for="regDate" class="form-label">등록일</label> <input
							type="text" class="form-control" name="createDate"
							th:value="${#dates.format(board.createDate, 'yyyy-MM-dd')}" readonly>
					</div>
					<div class="mb-3">
						<label for="writer" class="form-label">조회수</label> <input
							type="text" class="form-control" name="cnt"
							th:value="${board.cnt}">
					</div>

					<a href="list" class="btn btn-outline-secondary">목록으로</a> 
					<div th:if="${board.member.id == member.id}">
						<button class="btn btn-outline-warning" type="submit">게시글 수정</button>&nbsp;
						<button class="btn btn-outline-danger" th:formaction="@{/board/deleteBoard(seq=${board.seq})}" 
							type="submit">게시글 삭제</button>
					</div>
				</form>
			</div>
		</div>
	</div>
	</main>


	<!-- <h1>게시글 상세</h1>
	<form th:action="@{/board/updateBoard}" method="post">
		<table th:align="center" border="1" th:cellpadding="0"
			th:cellspacing="0">
			<tr>
				<td th:text="제목" width="80"></td>
				<td><input name="seq" type="hidden" th:value="${board.seq}">
					<input name="title" type="text" th:value="${board.title}"></td>
			</tr>
			<tr>
				<td th:text="작성자"></td>
				<td th:text="${board.member.name}"></td>
			</tr>
			<tr>
				<td th:text="내용">
				<td><textarea name="content" th:text="${board.content}"
						cols="40" rows="10"></textarea></td>
			</tr>
			<tr>
				<td th:text="등록일"></td>
				<td th:text="${#dates.format(board.createDate, 'yyyy-MM-dd')}">
			</tr>
			<tr>
				<td th:text="조회수"></td>
				<td th:text="${board.cnt}"></td>
			</tr>
			<tr th:if="${board.member.id == member.id}">
			작성자와 로그인한 사람을 비교해서 맞으면 수정, 삭제 버튼 활성화
				<td colspan="2" align="center">
					<button type="submit">게시글 수정</button>&nbsp;
					<button th:formaction="@{/board/deleteBoard(seq=${board.seq})}"
						type="submit">게시글 삭제</button>
				</td>
			</tr>
		</table>
	</form>
	<a th:href="@{/board/insertBoard}">글등록</a>&nbsp;&nbsp;&nbsp;
	<a sec:authorize="hasRole('ROLE_ADMIN')"
		th:href="@{/board/deleteBoard(seq=${board.seq})}">글삭제</a>&nbsp;&nbsp;&nbsp;
	<a th:href="@{/board/getBoardList}">글목록</a>
	<br />&nbsp;&nbsp;&nbsp;
 -->
	<div>Write a Comment</div>
	<form th:action="@{/board/insertComment}" method="post">
		<input name="board" type="hidden" th:value="${board.seq}">
		<!-- 테이블의 board 필드에 현재 게시글의 seq번호를 직접 입력... -->
		<div>
			<textarea name="comments" cols="50" rows="5" placeholder="댓글을 입력하세요"></textarea>
		</div>
		<div>
			<input type="submit" value="등록">
		</div>
		<!-- <div class="card-body" style="font-size: small"><a href="/auth/login">로그인</a>을 하시면 댓글을 등록할 수 있습니다.</div> 
            현재는 로그인을 해야 게시판에 접근할 수 있기 때문에 이 기능은 의미가 없다. 차우 로그인없이도 접근하게 만들 계획일때 다시 구현-->
	</form>
	</div>

	<!-- <div>
		<div>Comments</div>
		<ul th:each="comment, state : ${comment}">
			<li><span> <span th:text="${comment.member.id}"></span> <span
					th:text="${comment.createDate}"></span>&nbsp;
					<button>수정</button>
					<button>삭제</button>
			</span> <br />
				<div th:text="${comment.comments}"></div></li>
		</ul>
	</div> -->

	<div>
		<div>Comments</div>
		<div th:each="comment, state : ${comment}">
			<form th:action="@{/board/updateComment}" method="post">
				<table th:align="center" border="1">
					<tr>
						<td th:text="${comment.member.name}"></td>
						<td th:text="${comment.createDate}"></td>
						<td align="right" th:if="${comment.member.id == member.id}">
							<button type="submit">수정</button>&nbsp;
							<button th:formaction="@{/board/deleteComment}" type="submit">삭제</button>
							<!-- th:formaction == th:action의 주소를 오버라이딩 하는 개념 -->
					</tr>
					<br />
					<tr>
						<td th:align="center" colspan="3"><input name="seq"
							type="hidden" th:value="${comment.seq}"> <textarea
								name="comments" cols="50" th:text="${comment.comments}"></textarea>
							<input name="board" type="hidden" th:value="${board.seq}">
						</td>
					</tr>
				</table>
			</form>
		</div>
	</div>
	<!-- 댓글도 페이징 필요 -->

	<br />
	


</body>
</html>
